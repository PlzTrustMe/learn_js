'use strict'

// first task
const userInput = Number(prompt('Введите любое число: '));
let result;

// second task
result = 18 + userInput;

// third task
result -= 5;
result *= 5;
result /= 5;

// 4 task - No, it's const

// 5 task
alert(`This is the result - ${result}`);
alert(`This is user input - ${userInput}`);